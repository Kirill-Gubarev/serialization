#ifndef GUIDEFAULT_H
#define GUIDEFAULT_H

#include "gCode.h"
#include "element.h"

namespace gui{
	gCode nothing(const Element* self);
	gCode back(const Element* self);
	gCode iam(const Element* self);
}

#endif//GUIDEFAULT_H
